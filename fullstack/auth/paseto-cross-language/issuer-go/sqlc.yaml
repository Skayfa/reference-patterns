# Regenerate with: sqlc generate (from this directory). Output is committed.
version: "2"
sql:
  - engine: "sqlite"
    schema: "internal/store/migrations"
    queries: "internal/store/queries.sql"
    gen:
      go:
        package: "db"
        out: "internal/store/db"
