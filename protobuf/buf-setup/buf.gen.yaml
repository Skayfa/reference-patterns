# Codegen config — `buf generate` writes Go stubs to gen/ using remote
# plugins (no local protoc plugins to install).
version: v2
plugins:
  - remote: buf.build/protocolbuffers/go
    out: gen
    opt: paths=source_relative
  - remote: buf.build/grpc/go
    out: gen
    opt: paths=source_relative
