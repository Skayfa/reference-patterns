---
name: pattern-slug
language: typescript
category: testing
tags: [tag1, tag2]
description: One-line description — shown in llms.txt and in MCP search results
origin: source project + month the pattern was distilled from (e.g. `agentic-lab, 2026-07`; add the sillon subject slug if one exists; `built in-repo` for explorations)
test: command run by scripts/test-all.sh from this directory, after workspace install (e.g. `pnpm test`; use `none` for docs-only patterns)
---

# Pattern name

## Problem

What situation calls for this pattern.

## Solution

How the pattern solves it. Point to the files in this directory.

## Path

How the pattern was found — what was tried first, why it was rejected, the
decisive discovery. The part the final code cannot show; never invent it
after the fact.

## Key points

- The non-obvious decisions and gotchas — the things you would forget in six months.

## How to run

```bash
# the test command from the frontmatter
```
